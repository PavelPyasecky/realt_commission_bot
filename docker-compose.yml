services:
  bot:
    build: .
    env_file:
      - .env
    depends_on:
      - redis

  redis:
    image: redis:7
    ports:
      - "6379:6379"

  celery:
    build: .
    command: celery -A app.celery_app.celery_app worker -B --loglevel=INFO
    env_file:
      - .env
    depends_on:
      - redis
